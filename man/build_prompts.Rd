% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{build_prompts}
\alias{build_prompts}
\title{Build Nested Prompts for the LLM}
\usage{
build_prompts(
  scale.title,
  subject,
  audience,
  item_attributes,
  difficulty_level,
  item.examples
)
}
\arguments{
\item{scale.title}{A string representing the scale title (or \code{NULL}).}

\item{subject}{A string representing the subject (or \code{NULL}).}

\item{audience}{A string representing the audience (or \code{NULL}).}

\item{item_attributes}{A named list where each key is an item type and each value is a character vector of attributes.}

\item{difficulty_level}{A nested named list (with the same keys as \code{item_attributes}) where each attribute maps to a vector of difficulty levels (e.g., c("LOW", "MEDIUM", "HIGH")).}

\item{item.examples}{A validated data frame containing columns: \code{type}, \code{attribute}, \code{item}, and \code{answer}.}
}
\value{
A nested list of prompts, where the outer keys are item types, the inner keys are attributes, and the innermost keys are difficulty levels.
}
\description{
Constructs a nested list of main prompts for the LLM. The prompts are organized by:
\enumerate{
\item Item type (outer list; e.g., "fractions"),
\item Item attribute (inner list; e.g., "adding fractions"),
\item Difficulty level (innermost list; e.g., "MEDIUM").
}
}
\details{
For each prompt, the function extracts relevant examples from the \code{item.examples} data frame,
formats them appropriately, and then calls \code{build_main_prompt} with the provided parameters.
}
