% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_input_validation_helpers.R
\name{validate_item_examples_df}
\alias{validate_item_examples_df}
\title{Validate and Clean User Input Data Frame}
\usage{
validate_item_examples_df(item_examples, attributes)
}
\arguments{
\item{item_examples}{A data frame or \code{NULL}. If a data frame, must contain columns \code{attribute}, \code{type}, and \code{statement}.}

\item{attributes}{A named list of character vectors. Each name corresponds to a valid \code{type}, and each vector contains valid \code{attribute} names for that type.}
}
\value{
A cleaned and validated data frame with fixed casing and no duplicates, or \code{NULL} if \code{item_examples} is \code{NULL}.
}
\description{
Validates a user-provided data frame (\code{item_examples}) that should contain item definitions.
The input can be \code{NULL}, in which case it is returned as-is. If it is a data frame, it must contain
the required columns: \code{attribute}, \code{type}, and \code{statement} (all character type, no missing values),
and may optionally include \code{answer}. Rows are trimmed for whitespace, deduplicated (case-insensitively),
and validated against a known list of \code{attributes}.
}
